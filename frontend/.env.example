# Base URL of the backend API.
# In development the Vite dev server runs on :5173 and the API on :5000.
# In single-container production the API serves the built SPA, so leave this
# empty (same-origin requests) or set it to your deployed API origin.
VITE_API_URL=http://localhost:5000
